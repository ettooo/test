{
  "name": "progetto",
  "version": "1.0.0",
  "description": "YouTube to Shorts Converter",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}